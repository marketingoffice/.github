# HomeEstimator.ai — Brand Kit (verified)

Source: official brand kit screenshot supplied by the brand owner, 2026-09-23.
This file records the **authoritative** values. Where it disagrees with the
`brand-guard` skill's `references/homeestimator.md`, this file is correct and
that reference needs updating.

## Identity

| Field | Value |
|---|---|
| Display name (logo wordmark) | Home Estimator |
| Brand name in copy | HomeEstimator.ai |
| URL | https://www.homeestimator.ai/ |
| Logo | gold house + 4-point sparkle, "Home Estimator" beneath |

## Colors

| Hex | Role |
|---|---|
| `#1a1a2e` | primary dark background (navy) |
| `#2d2d44` | secondary dark / raised surfaces |
| `#c9a96e` | muted gold — body accents, borders |
| `#d4af37` | gold — headlines, CTAs, highlights |
| `#ffffff` | text on dark |

## Typography

- **Inter** — single family, all weights.

## Discrepancies with the brand-guard reference

The `brand-guard` skill's HomeEstimator.ai reference is out of date on three points:

1. **Background.** Reference says "deep black". Actual is navy `#1a1a2e` / `#2d2d44`.
   Measured across the six product screens, these two cover 58–90% of pixels.
2. **Gold.** Reference lists `#C9A84C` and `#F9E076`. The kit has `#c9a96e` and
   `#d4af37`. `#d4af37` is common to both; `#C9A84C` is a near-miss for `#c9a96e`
   (differs only in blue, 76 vs 110); `#F9E076` is not in the kit at all.
3. **Type.** Reference specifies Playfair Display headlines + DM Sans body for the
   HeyGen "Velvet Standard" style. The kit specifies Inter.

Rules in that reference NOT contradicted here, and still in force:
no "free" language; logo bottom-left and blended on every creative; primary CTA is
the 3-day money-back guarantee; rotate proof points; never framed as a home-value
or AVM tool; never connected to WA Construct.
